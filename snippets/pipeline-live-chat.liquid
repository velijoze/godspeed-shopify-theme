{% comment %}
  Pipeline Live Chat System
  Multi-platform chat integration with AI-powered responses
{% endcomment %}

{%- comment -%} Chat Widget Container {%- endcomment -%}
<div class="pipeline-chat-widget" id="pipeline-chat-widget">
  
  {%- comment -%} Chat Button {%- endcomment -%}
  <button class="chat-trigger" data-chat-toggle aria-label="Open chat">
    <div class="chat-icon">
      <svg class="icon-message" width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
        <path d="M20 2H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h4l4 4 4-4h4c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm-2 12H6v-2h12v2zm0-3H6V9h12v2zm0-3H6V6h12v2z"/>
      </svg>
      <svg class="icon-close" width="24" height="24" viewBox="0 0 24 24" fill="currentColor" style="display: none;">
        <path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/>
      </svg>
    </div>
    <span class="chat-text">Chat with us</span>
    <div class="chat-notification" data-chat-notification style="display: none;">
      <span class="notification-dot"></span>
    </div>
  </button>

  {%- comment -%} Chat Panel {%- endcomment -%}
  <div class="chat-panel" data-chat-panel>
    
    <div class="chat-header">
      <div class="chat-agent">
        <div class="agent-avatar">
          <img src="https://cdn.shopify.com/s/files/1/0533/2089/files/support-avatar.jpg?v=1642515494" 
               alt="Support Agent" 
               onerror="this.src='data:image/svg+xml;base64,'">
        </div>
        <div class="agent-info">
          <div class="agent-name">{{ section.settings.agent_name | default: 'E-Bike Expert' }}</div>
          <div class="agent-status">
            <span class="status-dot online"></span>
            <span class="status-text">Online now</span>
          </div>
        </div>
      </div>
      <button class="chat-close" data-chat-close>
        <svg width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
          <path d="M15 5L5 15M5 5L15 15" stroke="currentColor" stroke-width="2"/>
        </svg>
      </button>
    </div>

    <div class="chat-messages" data-chat-messages>
      <div class="message agent-message">
        <div class="message-avatar">
          <img src="data:image/svg+xml;base64," alt="Agent">
        </div>
        <div class="message-content">
          <div class="message-text">{{ section.settings.welcome_message | default: 'Hi! I\'m here to help you find the perfect e-bike. What questions do you have?' }}</div>
          <div class="message-time">now</div>
        </div>
      </div>
    </div>

    {%- comment -%} Quick Replies {%- endcomment -%}
    <div class="chat-quick-replies" data-quick-replies>
      <button class="quick-reply" data-quick-text="I'm looking for my first e-bike">
        🚴‍♂️ First e-bike
      </button>
      <button class="quick-reply" data-quick-text="What's the best commuter e-bike?">
        🏢 Commuting
      </button>
      <button class="quick-reply" data-quick-text="I need help with pricing and financing">
        💰 Pricing & financing
      </button>
      <button class="quick-reply" data-quick-text="Tell me about your warranty and returns">
        🛡️ Warranty & returns
      </button>
    </div>

    <div class="chat-input-container">
      <div class="chat-input-wrapper">
        <input type="text" 
               class="chat-input" 
               placeholder="Type your message..." 
               data-chat-input
               maxlength="500">
        <button class="chat-send" data-chat-send aria-label="Send message">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
            <path d="M2 10L18 2L12 10L18 18L2 10Z" stroke="currentColor" stroke-width="1.5" fill="none"/>
          </svg>
        </button>
      </div>
      <div class="chat-powered-by">
        <span>Powered by {{ section.settings.chat_provider | default: 'Pipeline Chat' }}</span>
      </div>
    </div>

  </div>

</div>

{%- comment -%} Third-party Chat Integration Points {%- endcomment -%}
{%- if section.settings.chat_platform == 'intercom' and section.settings.intercom_id != blank -%}
  <script>
    window.intercomSettings = {
      api_base: "https://widget.intercom.io",
      app_id: "{{ section.settings.intercom_id }}",
      custom_launcher_selector: '[data-chat-toggle]'
    };
  </script>
  <script>(function(){var w=window;var ic=w.Intercom;if(typeof ic==="function"){ic('reattach_activator');ic('update',w.intercomSettings);}else{var d=document;var i=function(){i.c(arguments);};i.q=[];i.c=function(args){i.q.push(args);};w.Intercom=i;var l=function(){var s=d.createElement('script');s.type='text/javascript';s.async=true;s.src='https://widget.intercom.io/widget/{{ section.settings.intercom_id }}';var x=d.getElementsByTagName('script')[0];x.parentNode.insertBefore(s,x);};if(document.readyState==='complete'){l();}else if(w.attachEvent){w.attachEvent('onload',l);}else{w.addEventListener('load',l,false);}}})();</script>
{%- endif -%}

{%- if section.settings.chat_platform == 'zendesk' and section.settings.zendesk_key != blank -%}
  <script id="ze-snippet" src="https://static.zdassets.com/ekr/snippet.js?key={{ section.settings.zendesk_key }}"></script>
{%- endif -%}

{%- if section.settings.chat_platform == 'crisp' and section.settings.crisp_id != blank -%}
  <script type="text/javascript">window.$crisp=[];window.CRISP_WEBSITE_ID="{{ section.settings.crisp_id }}";(function(){d=document;s=d.createElement("script");s.src="https://client.crisp.chat/l.js";s.async=1;d.getElementsByTagName("head")[0].appendChild(s);})();</script>
{%- endif -%}

{%- if section.settings.chat_platform == 'tidio' and section.settings.tidio_id != blank -%}
  <script src="//code.tidio.co/{{ section.settings.tidio_id }}.js" async></script>
{%- endif -%}

<style>
  .pipeline-chat-widget {
    position: fixed;
    bottom: 20px;
    right: 20px;
    z-index: 1000;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }

  .chat-trigger {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 1.5rem;
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    border: none;
    border-radius: 50px;
    cursor: pointer;
    box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
    transition: all 0.3s ease;
    position: relative;
  }

  .chat-trigger:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 35px rgba(102, 126, 234, 0.5);
  }

  .chat-trigger.active {
    border-radius: 12px;
    background: #374151;
  }

  .chat-icon {
    position: relative;
  }

  .chat-text {
    font-weight: 600;
    font-size: 0.9375rem;
  }

  .chat-notification {
    position: absolute;
    top: -5px;
    right: -5px;
  }

  .notification-dot {
    display: block;
    width: 12px;
    height: 12px;
    background: #ef4444;
    border-radius: 50%;
    border: 2px solid white;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .chat-panel {
    position: absolute;
    bottom: 70px;
    right: 0;
    width: 360px;
    height: 500px;
    background: white;
    border-radius: 16px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
    display: none;
    flex-direction: column;
    overflow: hidden;
  }

  .chat-panel.open {
    display: flex;
  }

  .chat-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.5rem;
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
  }

  .chat-agent {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .agent-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    overflow: hidden;
    border: 2px solid rgba(255, 255, 255, 0.3);
  }

  .agent-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .agent-name {
    font-weight: 600;
    font-size: 0.9375rem;
    margin-bottom: 0.25rem;
  }

  .agent-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8125rem;
    opacity: 0.9;
  }

  .status-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
  }

  .status-dot.online {
    background: #22c55e;
  }

  .status-dot.away {
    background: #f59e0b;
  }

  .status-dot.offline {
    background: #9ca3af;
  }

  .chat-close {
    background: none;
    border: none;
    color: white;
    cursor: pointer;
    padding: 0.5rem;
    border-radius: 50%;
    transition: background 0.2s ease;
  }

  .chat-close:hover {
    background: rgba(255, 255, 255, 0.2);
  }

  .chat-messages {
    flex: 1;
    padding: 1rem;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .message {
    display: flex;
    gap: 0.75rem;
    align-items: flex-start;
  }

  .message.user-message {
    flex-direction: row-reverse;
  }

  .message-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    overflow: hidden;
    flex-shrink: 0;
  }

  .message-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .message-content {
    max-width: 70%;
  }

  .user-message .message-content {
    text-align: right;
  }

  .message-text {
    padding: 0.75rem 1rem;
    border-radius: 16px;
    font-size: 0.9375rem;
    line-height: 1.4;
    word-wrap: break-word;
  }

  .agent-message .message-text {
    background: #f3f4f6;
    color: #374151;
    border-bottom-left-radius: 4px;
  }

  .user-message .message-text {
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    border-bottom-right-radius: 4px;
  }

  .message-time {
    font-size: 0.75rem;
    color: #9ca3af;
    margin-top: 0.25rem;
    padding: 0 0.5rem;
  }

  .chat-quick-replies {
    padding: 0 1rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .quick-reply {
    padding: 0.5rem 0.75rem;
    background: #f3f4f6;
    border: 1px solid #e5e7eb;
    border-radius: 20px;
    font-size: 0.8125rem;
    color: #374151;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .quick-reply:hover {
    background: #e5e7eb;
    border-color: #d1d5db;
  }

  .chat-input-container {
    padding: 1rem;
    border-top: 1px solid #e5e7eb;
  }

  .chat-input-wrapper {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: #f9fafb;
    border: 1px solid #e5e7eb;
    border-radius: 25px;
    padding: 0.5rem;
  }

  .chat-input {
    flex: 1;
    border: none;
    background: none;
    padding: 0.5rem 0.75rem;
    font-size: 0.9375rem;
    outline: none;
  }

  .chat-send {
    width: 36px;
    height: 36px;
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    border: none;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s ease;
  }

  .chat-send:hover {
    transform: scale(1.1);
  }

  .chat-send:disabled {
    background: #d1d5db;
    cursor: not-allowed;
    transform: none;
  }

  .chat-powered-by {
    text-align: center;
    margin-top: 0.5rem;
    font-size: 0.75rem;
    color: #9ca3af;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .pipeline-chat-widget {
      bottom: 15px;
      right: 15px;
    }

    .chat-trigger {
      padding: 0.875rem 1.25rem;
    }

    .chat-text {
      display: none;
    }

    .chat-panel {
      width: calc(100vw - 30px);
      height: calc(100vh - 120px);
      bottom: 60px;
      right: -15px;
    }
  }

  @media (max-width: 480px) {
    .chat-panel {
      width: 100vw;
      height: 100vh;
      bottom: 0;
      right: -20px;
      border-radius: 0;
    }
  }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const chatWidget = document.getElementById('pipeline-chat-widget');
  if (!chatWidget) return;

  const chatTrigger = chatWidget.querySelector('[data-chat-toggle]');
  const chatPanel = chatWidget.querySelector('[data-chat-panel]');
  const chatClose = chatWidget.querySelector('[data-chat-close]');
  const chatInput = chatWidget.querySelector('[data-chat-input]');
  const chatSend = chatWidget.querySelector('[data-chat-send]');
  const chatMessages = chatWidget.querySelector('[data-chat-messages]');
  const quickReplies = chatWidget.querySelectorAll('[data-quick-text]');

  // Toggle chat panel
  function toggleChat() {
    const isOpen = chatPanel.classList.contains('open');
    if (isOpen) {
      closeChat();
    } else {
      openChat();
    }
  }

  function openChat() {
    chatPanel.classList.add('open');
    chatTrigger.classList.add('active');
    chatTrigger.querySelector('.icon-message').style.display = 'none';
    chatTrigger.querySelector('.icon-close').style.display = 'block';
    chatInput.focus();
    
    // Hide quick replies after first interaction
    setTimeout(() => {
      const quickRepliesContainer = chatWidget.querySelector('[data-quick-replies]');
      if (quickRepliesContainer && chatMessages.children.length > 1) {
        quickRepliesContainer.style.display = 'none';
      }
    }, 5000);
  }

  function closeChat() {
    chatPanel.classList.remove('open');
    chatTrigger.classList.remove('active');
    chatTrigger.querySelector('.icon-message').style.display = 'block';
    chatTrigger.querySelector('.icon-close').style.display = 'none';
  }

  // Send message
  function sendMessage(text) {
    if (!text.trim()) return;

    // Add user message
    addMessage(text, 'user');
    
    // Clear input
    chatInput.value = '';
    
    // Show typing indicator
    showTypingIndicator();
    
    // Simulate response (replace with actual chat API)
    setTimeout(() => {
      hideTypingIndicator();
      handleAutoResponse(text);
    }, 1000 + Math.random() * 2000);
  }

  function addMessage(text, sender) {
    const message = document.createElement('div');
    message.className = `message ${sender}-message`;
    
    const avatar = document.createElement('div');
    avatar.className = 'message-avatar';
    
    if (sender === 'user') {
      avatar.innerHTML = '<img src="data:image/svg+xml;base64," alt="You">';
    } else {
      avatar.innerHTML = '<img src="data:image/svg+xml;base64," alt="Agent">';
    }
    
    const content = document.createElement('div');
    content.className = 'message-content';
    content.innerHTML = `
      <div class="message-text">${text}</div>
      <div class="message-time">${new Date().toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}</div>
    `;
    
    message.appendChild(avatar);
    message.appendChild(content);
    chatMessages.appendChild(message);
    
    // Scroll to bottom
    chatMessages.scrollTop = chatMessages.scrollHeight;
  }

  function showTypingIndicator() {
    const indicator = document.createElement('div');
    indicator.className = 'message agent-message typing-indicator';
    indicator.innerHTML = `
      <div class="message-avatar">
        <img src="data:image/svg+xml;base64," alt="Agent">
      </div>
      <div class="message-content">
        <div class="message-text">
          <span>●</span><span>●</span><span>●</span>
        </div>
      </div>
    `;
    
    chatMessages.appendChild(indicator);
    chatMessages.scrollTop = chatMessages.scrollHeight;
  }

  function hideTypingIndicator() {
    const indicator = chatMessages.querySelector('.typing-indicator');
    if (indicator) {
      indicator.remove();
    }
  }

  // Simple auto-responses (replace with real chat API)
  function handleAutoResponse(userMessage) {
    const message = userMessage.toLowerCase();
    let response = "Thanks for your message! I'll connect you with one of our e-bike experts who can help you find the perfect electric bike for your needs.";

    if (message.includes('first') || message.includes('beginner')) {
      response = "Great choice getting your first e-bike! I'd recommend starting with our commuter models - they're user-friendly and perfect for learning. Would you like me to show you our top beginner-friendly options?";
    } else if (message.includes('commut')) {
      response = "For commuting, you'll want an e-bike with good range and comfort. Our commuter models offer 40-60 mile range and are built for daily use. What's your typical commute distance?";
    } else if (message.includes('price') || message.includes('cost') || message.includes('financ')) {
      response = "We offer e-bikes starting from $1,200 with financing options available. We also have seasonal sales and bundle deals. What's your budget range? I can show you the best options in your price range.";
    } else if (message.includes('warrant') || message.includes('return')) {
      response = "We offer comprehensive warranties on all e-bikes (2-3 years on batteries, varies by manufacturer on components) plus a 30-day return policy. All bikes come with free shipping over $100 and professional assembly guidance.";
    }

    addMessage(response, 'agent');
  }

  // Event listeners
  chatTrigger.addEventListener('click', toggleChat);
  chatClose.addEventListener('click', closeChat);

  chatSend.addEventListener('click', () => {
    sendMessage(chatInput.value);
  });

  chatInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') {
      sendMessage(chatInput.value);
    }
  });

  // Quick reply buttons
  quickReplies.forEach(button => {
    button.addEventListener('click', () => {
      const text = button.dataset.quickText;
      sendMessage(text);
      button.parentElement.style.display = 'none';
    });
  });

  // Close chat when clicking outside
  document.addEventListener('click', (e) => {
    if (!chatWidget.contains(e.target) && chatPanel.classList.contains('open')) {
      closeChat();
    }
  });

  // Show notification after delay
  setTimeout(() => {
    const notification = chatWidget.querySelector('[data-chat-notification]');
    if (notification && !chatPanel.classList.contains('open')) {
      notification.style.display = 'block';
    }
  }, 10000);
});
</script>