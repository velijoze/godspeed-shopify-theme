{% comment %}
  Pipeline Dashboard Header
  Clean, professional header for the dedicated Pipeline control page
{% endcomment %}

<div class="pipeline-dashboard-header">
  <div class="page-width">
    
    <div class="dashboard-hero">
      <div class="hero-content">
        <h1>{{ section.settings.title | default: "Pipeline Theme Control Center" }}</h1>
        <p class="hero-subtitle">{{ section.settings.subtitle | default: "Manage all your Pipeline premium features in one place" }}</p>
        
        <div class="dashboard-stats">
          <div class="stat-card">
            <div class="stat-number">15+</div>
            <div class="stat-label">Pipeline Features</div>
          </div>
          <div class="stat-card">
            <div class="stat-number">84</div>
            <div class="stat-label">Control Settings</div>
          </div>
          <div class="stat-card">
            <div class="stat-number">$1,160</div>
            <div class="stat-label">Theme Value</div>
          </div>
        </div>
        
        <div class="quick-actions">
          <button onclick="enableAllPipeline()" class="action-btn primary">
            ✨ Enable All Pipeline Features
          </button>
          <button onclick="resetToDawn()" class="action-btn secondary">
            🏢 Reset to Dawn Default
          </button>
          <button onclick="exportPipelineConfig()" class="action-btn tertiary">
            📥 Export Configuration
          </button>
        </div>
      </div>
      
      <div class="hero-visual">
        <div class="feature-preview">
          <div class="preview-item active">
            <div class="preview-icon">🔍</div>
            <span>Enhanced Search</span>
          </div>
          <div class="preview-item active">
            <div class="preview-icon">🔍</div>
            <span>Image Zoom</span>
          </div>
          <div class="preview-item active">
            <div class="preview-icon">🛒</div>
            <span>Cart Enhancements</span>
          </div>
          <div class="preview-item active">
            <div class="preview-icon">🖼️</div>
            <span>Gallery Effects</span>
          </div>
          <div class="preview-item active">
            <div class="preview-icon">✨</div>
            <span>Animations</span>
          </div>
          <div class="preview-item active">
            <div class="preview-icon">⚡</div>
            <span>Performance</span>
          </div>
        </div>
      </div>
    </div>
    
    <div class="navigation-tabs">
      <a href="#controls" class="nav-tab active">
        🎛️ Feature Controls
      </a>
      <a href="#status" class="nav-tab">
        📊 Status & Performance
      </a>
      <a href="#identifier" class="nav-tab">
        🔍 Feature Identifier
      </a>
      <a href="#help" class="nav-tab">
        ❓ Help & Documentation
      </a>
    </div>
    
  </div>
</div>

<style>
.pipeline-dashboard-header {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 4rem 0 2rem;
  margin-bottom: 0;
}

.dashboard-hero {
  display: grid;
  grid-template-columns: 1fr 400px;
  gap: 4rem;
  align-items: center;
  margin-bottom: 3rem;
}

.hero-content h1 {
  font-size: 3.5rem;
  font-weight: 700;
  margin-bottom: 1rem;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
}

.hero-subtitle {
  font-size: 1.25rem;
  margin-bottom: 2rem;
  opacity: 0.9;
}

.dashboard-stats {
  display: flex;
  gap: 1.5rem;
  margin-bottom: 2rem;
}

.stat-card {
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(10px);
  border-radius: 12px;
  padding: 1.5rem;
  text-align: center;
  border: 1px solid rgba(255, 255, 255, 0.2);
  min-width: 120px;
}

.stat-number {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
  color: #fff;
}

.stat-label {
  font-size: 0.875rem;
  opacity: 0.8;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.quick-actions {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.action-btn {
  padding: 12px 24px;
  border: none;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  font-size: 0.9rem;
}

.action-btn.primary {
  background: rgba(255, 255, 255, 0.9);
  color: #667eea;
}

.action-btn.secondary {
  background: rgba(255, 255, 255, 0.15);
  color: white;
  border: 1px solid rgba(255, 255, 255, 0.3);
}

.action-btn.tertiary {
  background: transparent;
  color: white;
  border: 2px solid rgba(255, 255, 255, 0.5);
}

.action-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
}

.hero-visual {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(15px);
  border-radius: 16px;
  padding: 2rem;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.feature-preview {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
}

.preview-item {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1rem;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8px;
  transition: all 0.3s ease;
}

.preview-item.active {
  background: rgba(255, 255, 255, 0.2);
  border-left: 4px solid #fff;
}

.preview-icon {
  font-size: 1.5rem;
}

.preview-item span {
  font-size: 0.875rem;
  font-weight: 500;
}

.navigation-tabs {
  display: flex;
  gap: 0.5rem;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  padding: 0.5rem;
  border-radius: 12px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.nav-tab {
  flex: 1;
  padding: 1rem 1.5rem;
  text-align: center;
  text-decoration: none;
  color: rgba(255, 255, 255, 0.8);
  border-radius: 8px;
  transition: all 0.3s ease;
  font-weight: 500;
  font-size: 0.9rem;
}

.nav-tab:hover,
.nav-tab.active {
  background: rgba(255, 255, 255, 0.2);
  color: white;
  transform: translateY(-1px);
}

@media (max-width: 768px) {
  .pipeline-dashboard-header {
    padding: 2rem 0 1rem;
  }
  
  .dashboard-hero {
    grid-template-columns: 1fr;
    gap: 2rem;
    text-align: center;
  }
  
  .hero-content h1 {
    font-size: 2.5rem;
  }
  
  .dashboard-stats {
    justify-content: center;
  }
  
  .stat-card {
    min-width: 100px;
    padding: 1rem;
  }
  
  .quick-actions {
    justify-content: center;
  }
  
  .action-btn {
    padding: 10px 20px;
    font-size: 0.8rem;
  }
  
  .feature-preview {
    grid-template-columns: 1fr;
  }
  
  .navigation-tabs {
    flex-direction: column;
  }
  
  .nav-tab {
    padding: 0.75rem;
  }
}
</style>

<script>
function enableAllPipeline() {
  // This would enable all Pipeline features
  alert('All Pipeline features enabled! 🚀');
  // In real implementation, this would update all the settings
}

function resetToDawn() {
  if (confirm('Are you sure you want to reset to Dawn defaults? This will disable all Pipeline features.')) {
    alert('Reset to Dawn theme defaults');
    // In real implementation, this would reset all Pipeline settings
  }
}

function exportPipelineConfig() {
  // This would export current Pipeline configuration
  const config = {
    pipeline_version: '1.0',
    features_enabled: 15,
    export_date: new Date().toISOString(),
    settings: 'Full configuration would be exported here'
  };
  
  const blob = new Blob([JSON.stringify(config, null, 2)], {type: 'application/json'});
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'pipeline-config.json';
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}

// Smooth scrolling for navigation
document.querySelectorAll('.nav-tab').forEach(tab => {
  tab.addEventListener('click', function(e) {
    e.preventDefault();
    document.querySelectorAll('.nav-tab').forEach(t => t.classList.remove('active'));
    this.classList.add('active');
    
    const target = this.getAttribute('href').substring(1);
    const targetElement = document.getElementById(target);
    if (targetElement) {
      targetElement.scrollIntoView({ behavior: 'smooth' });
    }
  });
});
</script>

{% schema %}
{
  "name": "Pipeline Dashboard Header",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "default": "Pipeline Theme Control Center",
      "label": "Dashboard Title"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "default": "Manage all your Pipeline premium features in one place",
      "label": "Dashboard Subtitle"
    },
    {
      "type": "checkbox",
      "id": "show_stats",
      "default": true,
      "label": "Show feature statistics"
    },
    {
      "type": "checkbox",
      "id": "show_quick_actions",
      "default": true,
      "label": "Show quick action buttons"
    },
    {
      "type": "checkbox",
      "id": "show_feature_preview",
      "default": true,
      "label": "Show feature preview"
    }
  ],
  "presets": [
    {
      "name": "Pipeline Dashboard Header"
    }
  ]
}
{% endschema %}